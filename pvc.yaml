apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: namespace-name
  name: pvc
  labels:
    name: pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
